<div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="bg-white shadow rounded-lg">
    <div class="px-4 py-5 sm:p-6">
      <h1 class="text-2xl font-bold text-gray-900 mb-2">Edit Shift in <%= @roster.name %></h1>
      <p class="text-sm text-gray-600 mb-6">Update this shift template details</p>

      <%= render "form", roster: @roster, base_shift: @base_shift %>
    </div>
  </div>
</div>
